name: storiez
description: A secret message sharing stories app.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.16.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  get_it: ^7.1.3
  flutter_dialog_manager: ^1.0.0+4
  gap: ^2.0.0
  equatable: ^2.0.3
  flutter_secure_storage: ^5.0.2
  shared_preferences: ^2.0.13
  riverpod: ^1.0.3
  flutter_riverpod: ^1.0.3
  google_fonts: ^3.0.1
  flutter_phosphor_icons: ^0.0.1+4
  firebase_core: ^1.17.1
  firebase_auth: ^3.3.19
  cloud_firestore: ^3.1.17
  firebase_storage: ^10.2.17
  uuid: ^3.0.6
  steganograph:
    git:
      url: git@github.com:Crazelu/steganograph.git
      ref: main
  
dependency_overrides:
  xml: ^6.0.0
  collection: ^1.16.0

dev_dependencies:
  # flutter_test:
  #   sdk: flutter
  flutter_lints: ^1.0.0

# The following section is specific to Flutter.
flutter:
  uses-material-design: true

  assets: 
    - assets/
